import React from 'react'

function Theory() {
  return (
    <div>
        State management means handling and controlling data in a react application so that 
        the UI updates whenever the data changes.
        State: data that changes over time in the app
        State Management: the way we store, update, and share that data between components
        
        Need of state management:
        store dynamic data
        update UI when data changes
        share data between components
        avoid unnecessary prop drilling
        keep application data organized
    </div>
  )
}

export default Theory