import React from 'react'

function ExternalState() {
  return (
    <div>
        {/* for large applications, external libraries are used to manage complex state */}
        {/* use when:
        application has complex shared data
        many components depend on same data
        state updates become difficult with only props/context */}
        {/* ContextApi
        Redux
        ReduxToolkit */}
    </div>
  )
}

export default ExternalState