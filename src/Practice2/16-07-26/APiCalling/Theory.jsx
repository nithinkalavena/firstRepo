import React from 'react'

function Theory() {
  return (
    <div>
        API calling in React means sending requests from your React application to a backend server
        or external service to fetch, create, update, or delete data. The server responds with data
        (usually in JSON format), which your React components use to update the UI

        For example:
        A weather app fetches the current weather from a weather API.
        An e-commerce app fetches a list of products.
        A social media app sends a new post to the server
    </div>
  )
}

export default Theory