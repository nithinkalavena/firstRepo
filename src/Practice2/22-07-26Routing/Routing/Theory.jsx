import React from 'react'

function Theory() {
  return (
    <div>
        React Routing allows you to navigate between different pages (components)
        in a React application without reloading the browser.

        ReactRouter: is a library that enables navigation between different components in a react application
        without reloading the page.
        BrowserRouter: is the main router component. it uses the browser's url and history api to manage navigation.
        Routes: is a container that holds all the route definations
        Route: maps a url path to a react component
        Link: is used to navigate between pages without refreshing the browser
    </div>
  )
}

export default Theory