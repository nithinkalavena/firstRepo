import React from 'react'

function Theory() {
  return (
    <div>
        Destructuring in React is a JavaScript feature that makes it easier to extract values
        from objects and arrays. It's commonly used in React to work with props, state, and hook return values.
    </div>
  )
}

export default Theory