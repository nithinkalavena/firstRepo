import React from 'react'

function Theory() {
  return (
    <div>
        There are several ways to style components in React. The most common approaches are:

        1. CSS Stylesheets (Most Common)
        Create a CSS file.
        2. Inline Styles
        Styles are written as JavaScript objects    
        3. CSS Modules    
        CSS Modules scope styles to a component, avoiding class name conflicts.
        4. Styled Components
        A CSS-in-JS library that lets you write CSS inside JavaScript.
        5. Tailwind CSS
        Uses utility classes directly in JSX.
        6. Dynamic Styling
        Apply styles conditionally based on state or props.        
    </div>
  )
}

export default Theory