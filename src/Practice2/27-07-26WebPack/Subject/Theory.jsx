import React from 'react'

function Theory() {
  return (
    <div>
        Webpack
        Webpack is a module bundler.

        Its job is to:
        Combine all your JavaScript files into one (or a few) bundles
        Process CSS, images, fonts, and other assets
        Convert modern JavaScript (ES6+) using Babel
        Optimize your application for production

        Webpack bundles everything into files that the browser can load efficiently.
    </div>
  )
}

export default Theory