import React from 'react'

function Intro() {
  return (
    <div>
        React is a javascript library used to build user interfaces (UI), especially for web aplications
        Components: are reusable building blocks of a react application
        types:
        fuctional components(modern)
        Class components(older)
        Hooks: are special functions that let you use state and lifecycle features in functional components
        Props: are used to pass data from one to another component
    </div>
  )
}

export default Intro